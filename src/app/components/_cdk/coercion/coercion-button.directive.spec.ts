import { CoercionButtonDirective } from './coercion-button.directive';

describe('CoercionButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CoercionButtonDirective();
    expect(directive).toBeTruthy();
  });
});
